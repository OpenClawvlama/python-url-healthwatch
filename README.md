# python-url-healthwatch

Мини-проект на Python для проверки доступности сайтов

## Что умеет
- проверяет список URL
- выводит статус-коды и задержку
- сохраняет отчёт в JSON

## Запуск

```bash
python main.py --url https://example.com --url https://github.com --out report.json
```
